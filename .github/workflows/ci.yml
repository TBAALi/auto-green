name: ci

on:
  push:
    branches:
      - main
  schedule:
    - cron: "* * * * *"

jobs:
  autogreen:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repository
        uses: actions/checkout@v3

      - name: Auto green
        run: |
          git config --local user.email "jiaxx903@foxmail.com"
          git config --local user.name "TBAALi"
          git clone https://github.com/TBAALi/glog.git
          git pull --rebase
          git commit --allow-empty -m "a commit a day keeps your girlfriend away"
          git push
